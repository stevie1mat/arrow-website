import { SectionProps } from "@/types/sectionProps";

export const PricingPlanData: SectionProps = {
    wrapperCls: "mt-100",
    container: "container",
    subheading: "Extended Support Plans",
    heading: "Support & Maintenance",
}