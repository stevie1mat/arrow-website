import { SectionProps } from "@/types/sectionProps";

export const FeaturedProjectData: SectionProps = {
    wrapperCls: "mt-100",
    container: "container",
    subheading: "Recent Projects",
    heading: "Explore the Recent Works We Have Done!",
}