import { SectionProps } from "@/types/sectionProps";

export const TeamSlider2Data: SectionProps = {
    wrapperCls: "mt-100 mb-100",
    container: "container",
    subheading: "Our Team",
    heading: "Meet the experts behind your success",
}
