import { SectionProps } from "@/types/sectionProps";

export const TestimonialData: SectionProps = {
    wrapperCls: "testimonial testimonial-about mt-100 section-padding",
    container: "container",
    subheading: "Testimonial",
    heading: "See what our customers have to say about us",
}